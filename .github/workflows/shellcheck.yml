name: shellcheck

on:
  push:
  pull_request:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ShellCheck
        run: shellcheck bootstrap.sh novo-cliente.sh revogar-cliente.sh backup-pki.sh lib/common.sh
